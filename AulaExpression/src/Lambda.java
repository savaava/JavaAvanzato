public class Lambda{
    //è detta anche funzione anonima
    //è una funzione senza nome definia al volo

    /*
     * new Thread(new Runnable (){
     * @Override
     * public void run(){
     * System.out.print("Ciao gay");}
     * }).start();
     * 
     * oppure 
     * //lambda funzionano solo con le interfacce funzionali 
     * new Thread(()-> System.ot.println("ciao gay").start(););
     * 
     * SINTASSI
     * [([lista parametri])]-> [{blocco di codice}]
     * le variabili sono in sola lettura --> utilizzate all''interno di un metodo
     * 
     * si possono asegnare dei refernce a espressioni lambda
     * Runnable r= ()->{};--> non si può usare var
     * 
     * QUANDO USARLE
     * 
     */
}